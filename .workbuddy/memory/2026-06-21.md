# 2026-06-21 工作日志

## 修复登录"网络错误" + 密码明文问题
- **根因**：数据库中存储的是明文密码，但后端用 BCrypt.matches() 验证 → 永远匹配失败 → 后端抛异常 → 前端显示笼统的"网络错误"
- **修复 1**：新建 `DataInitializer.java` — 首次启动自动创建 admin 账号（密码 BCrypt 加密）
- **修复 2**：新建 `init.sql` — 完整建表语句（含 users/articles/comments/likes 四张表）
- **修复 3**：改进前端 `request.ts` 错误处理 — 不再笼统显示"网络错误"，区分无响应/HTTP错误/业务异常
- **修复 4**：新建 `fix-login.sh` 服务器诊断修复脚本（check/fix/rebuild/logs 四个命令）
- 部署方式：Linux 服务器 docker-compose（bash start.sh prod 一键部署），访问 http://180.76.133.234/
