# 2026-06-21 工作日志

## 修复登录"网络错误" + 密码明文问题
- **根因**：数据库中存储的是明文密码，但后端用 BCrypt.matches() 验证 → 永远匹配失败 → 后端抛异常 → 前端显示笼统的"网络错误"
- **修复 1**：新建 `DataInitializer.java` — 首次启动自动创建 admin 账号（bcrypt(sha256) 双重加密）
- **修复 2**：新建 `init.sql` — 完整建表语句
- **修复 3**：改进前端 `request.ts` 错误处理 — 区分无响应/HTTP状态码/业务错误
- **修复 4**：新建 `fix-login.sh` 服务器诊断修复脚本

## 前端传输加密（SHA-256）
- **方案**：前端 SHA-256(password) → 后端 BCrypt(SHA-256) 存储
- **新增** `utils/crypto.ts`：Web Crypto API 优先，HTTP 环境降级 js-sha256 库
- **修改** `LoginView.vue` + `RegisterView.vue`：提交前 encryptPassword()
- **验证**：5 个测试用例（含中文）js-sha256 与 Node.js 标准完全一致 ✅
## 新增文章编辑功能
- **后端**：`ArticleServiceImpl.updateArticle()` 加 admin 权限（与删除一致）
- **前端路由**：新增 `/write/:id` 路由，`WriteView.vue` 改为同时支持新建和编辑
- **WriteView.vue**：检测 `:id` 参数 → 编辑模式（拉取文章回填表单 → 提交调 update → 跳回详情页）
- **ArticleDetailView.vue**：加编辑按钮（✎ EDIT），作者/admin 可见
- **ArticleCard.vue**：加编辑按钮（✎ EDIT），hover 显示，作者/admin 可见
- **前端构建**：vite build 通过 ✅
- **根因**：DataInitializer 发现 admin 已存在就 `return`，不更新密码。旧 admin 密码格式是 `bcrypt(明文)`，新前端传的是 `sha256(明文)` → 永远匹配不上
- **修复**：改为已存在时也强制更新密码为 `bcrypt(sha256)` 格式（第60-68行）
- **效果**：每次重启后端都会自动修正 admin 密码格式，无需手动清库

## Docker 本地全功能验证（--no-cache 构建）
- **构建**：docker compose build --no-cache ✅（前端 vite build + 后端 Maven 全部通过）
- **容器**：5 个容器全部启动 + healthy ✅
- **测试结果**：
  - 管理员登录（SHA-256 → bcrypt 双重加密） ✅
  - nginx 代理转发（:80 → :8080） ✅
  - 文章创建（含中文标题/内容/标签） ✅
  - 文章编辑（PUT update title/content/keywords） ✅
  - 前端页面（WriteView 编辑功能代码已打包） ✅
- **注意**：Windows curl 发送中文 JSON 有 UTF-8 编码问题，需用 PowerShell Invoke-RestMethod 替代

## 文章卡片底部布局优化
- **问题**：编辑/删除按钮挤在 meta 行（作者/日期同一行）、标签和统计信息挤在一起、卡片高度不统一
- **重构** `ArticleCard.vue`：
  - 编辑/删除按钮从 `.card-meta` 移到 `.card-footer` 的独立 `.card-actions` 区域（hover 显示）
  - 底部分为两行：**footer-top**（标签 + 操作按钮）+ **footer-bottom**（浏览/点赞/评论统计）
  - 卡片改为 `flex-column` 布局，摘要区域 `flex: 1` 自动填充，保证同一行卡片等高
  - 按钮改为方形图标按钮（26×26px），更紧凑美观
  - footer 区域加顶部分隔线 `border-top`
  - 封面图高度从 180px 缩至 160px，整体更紧凑
- **构建验证**：vite build ✅ → docker --no-cache rebuild ✅ → 新 JS bundle 已生效
