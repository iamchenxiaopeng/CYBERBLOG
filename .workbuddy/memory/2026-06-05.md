# 2026-06-05 工作日志

## 今日完成

### 赛博朋克博客 - 匿名点赞功能

**问题**：文章详情页点赞按钮点击无反应（未登录时直接 return，无提示）

**修复**：
1. **前端** `ArticleDetailView.vue`：`toggleLike` 加 `try-catch`，未登录时 Toast 提示"请先登录后点赞"
2. **后端** `Like.java` 实体：新增 `guestId` 字段（未登录用户标识）
3. **后端** `LikeController.java`：支持 `X-Guest-Id` 请求头，匿名用户也可点赞
4. **后端** `LikeServiceImpl.java`：`toggleLike` 同时支持 `userId`（登录）和 `guestId`（匿名）
5. **后端** `SecurityConfig.java`：`/api/likes/**` 放行（允许匿名访问）
6. **数据库** `likes` 表迁移：
   - 新增 `guest_id VARCHAR(64)` 列
   - `user_id` 改为允许 NULL
   - 新增 `uk_like_guest (guest_id, target_id, target_type)` 唯一索引
   - 保留 `uk_like_user (user_id, target_id, target_type)` 唯一索引
7. **前端** 新增 `utils/guest.ts`：生成并存储 UUID 作为 guestId（localStorage）
8. **前端** `api/interact.ts`：`likeApi.toggle` 未登录时自动带 `X-Guest-Id` 请求头

**验证**：
- 后端重启后 API 正常（端口 8080）
- 匿名点赞切换测试通过（POST /api/likes/article/4，X-Guest-Id: test-guest-001）
- 前端 5173 端口正常

**遗留**：后端需用 `--server.port=8080` 参数启动（application.yml 的 server.port 未生效，原因待查）
