# 2026-06-16 工作日志

## 今日完成

### 文章删除功能（作者/admin 可删）

**需求**：在文章卡片和详情页添加删除按钮，用户可删自己的文章，管理员（username=admin）可删所有人。

**改动文件**：
1. **后端** `ArticleServiceImpl.java`：`deleteArticle()` 新增 admin 判断——查询当前用户 username 是否为 "admin"，是则跳过权限校验
2. **前端** `ArticleCard.vue`：卡片 meta 区域新增 "✕ DELETE" 按钮，hover 显示，仅作者/管理员可见；点击弹出确认框后调用 API 并 emit `deleted` 事件
3. **前端** `ArticleDetailView.vue`：文章 header 区域新增 "✕ DELETE ARTICLE" 按钮，仅作者/管理员可见
4. **前端** `ArticleListView.vue`：监听 ArticleCard 的 `@deleted` 事件，从列表中移除对应条目
5. **数据库**：注册了 admin 账号（id=5, 密码 Admin@2077）

**权限验证结果**：
| 操作者 | 目标 | 结果 |
|--------|------|------|
| cyberadmin (id=1) | 自己的文章 | 200 ✅ |
| admin (id=5) | 别人的文章 | 200 ✅ |
| cyberadmin (id=1) | admin 的文章 | 403 无权限 ✅ |

**重建验证**：`docker compose up --build -d backend frontend` 构建成功，前后端均正常启动。
