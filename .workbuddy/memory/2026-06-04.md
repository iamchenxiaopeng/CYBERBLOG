# 2026-06-04 工作日志

## 完成：赛博朋克博客全栈项目初始化

### 项目位置
`D:/1study/myblog/`

### 已生成内容（共 67 个文件）

**基础设施**
- `docker-compose.yml`：MySQL 8 + Redis 7 + MinIO 一键启动
- `backend/src/main/resources/init.sql`：建表 SQL (users/articles/comments/likes)

**后端 Spring Boot 3 (Java 17)**
- 认证：JWT 无状态认证，Spring Security 6，一键注册即登录
- 文章：CRUD + MD 文件上传到 MinIO + 自动摘要提取
- 互动：点赞切换（article/comment）+ 二级评论
- 通用：统一 Result<T> 返回，全局异常处理，MyBatis-Plus 分页

**前端 Vue 3 + Vite + TypeScript**
- 赛博朋克 CSS 主题（扫描线、霓虹、Glitch 动效）
- 页面：首页、文章列表、文章详情(MD渲染+点赞+评论)、登录、注册、写作页(手写/上传MD)、404
- Pinia user store + JWT 持久化 + Axios 拦截器

### 启动命令
1. `docker-compose up -d`（中间件）
2. `cd backend && mvn clean package -DskipTests && java -jar target/cyberblog-backend-1.0.0.jar`
3. `cd frontend && npm install && npm run dev`
